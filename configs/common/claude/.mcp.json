{
  "mcpServers": {
    "nixos": {
      "command": "bash",
      "args": [
        "-lc",
        "if command -v mcp-nixos >/dev/null 2>&1; then exec mcp-nixos; else exec nix run github:utensils/mcp-nixos --; fi"
      ],
      "type": "stdio"
    }
  }
}
