# Copy to hermes-openai.env, fill values, then encrypt in place:
# just sops-encrypt-env file='secrets/hermes-openai.env'
# Required for direct OpenAI auxiliary tasks (for example, compression)
OPENAI_API_KEY=***
# Optional if you want to pin a non-default OpenAI-compatible endpoint
# OPENAI_BASE_URL=https://api.openai.com/v1
